'use strict';

describe('eKlub.fees module', function() {

  beforeEach(module('eKlub.fees'));

  describe('fees controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var feesCtrl = $controller('FeesController');
      expect(feesCtrl).toBeDefined();
    }));

  });
});