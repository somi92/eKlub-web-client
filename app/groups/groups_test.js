'use strict';

describe('eKlub.groups module', function() {

  beforeEach(module('eKlub.groups'));

  describe('groups controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var groupsCtrl = $controller('GroupsController');
      expect(groupsCtrl).toBeDefined();
    }));

  });
});