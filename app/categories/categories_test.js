'use strict';

describe('eKlub.categories module', function() {

  beforeEach(module('eKlub.categories'));

  describe('categories controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var categoriesCtrl = $controller('CategoriesController');
      expect(categoriesCtrl).toBeDefined();
    }));

  });
});