'use strict';

describe('eKlub.members module', function() {

  beforeEach(module('eKlub.members'));

  describe('members controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var membersCtrl = $controller('MembersController');
      expect(membersCtrl).toBeDefined();
    }));

  });
});